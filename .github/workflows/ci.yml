name: CI

on:
  push:
    branches: [main, develop]
  pull_request:
    branches: [main]

jobs:
  quality:
    name: Lint & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Set up uv
        uses: astral-sh/setup-uv@v3

      - name: Install dev dependencies
        run: uv sync --group dev

      - name: Lint (ruff)
        run: uv run ruff check src/churn tests

      - name: Format check (ruff)
        run: uv run ruff format --check src/churn tests

      - name: Type check (mypy)
        run: uv run mypy src/churn --ignore-missing-imports

  test:
    name: Tests
    runs-on: ubuntu-latest
    needs: quality
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Set up uv
        uses: astral-sh/setup-uv@v3

      - name: Install dev dependencies
        run: uv sync --group dev

      - name: Run unit tests
        run: uv run pytest tests/unit -v --cov=src/churn --cov-report=xml

      - name: Run data quality (leakage) tests
        run: uv run pytest tests/data_quality -v

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
